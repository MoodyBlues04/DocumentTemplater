services:
  app:
    image: document_templater
    ports:
      - "8080:80"
    env_file:
      - .env
    depends_on:
      - db

  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: testing
      POSTGRES_USER: sail
      POSTGRES_PASSWORD: secret
